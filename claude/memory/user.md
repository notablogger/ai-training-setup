---
name: user-profile
description: "Who Nikhil is — learning AI through building real projects, documentation-heavy, values understanding \"why\" not just \"what\""
metadata: 
  node_type: memory
  type: user
  originSessionId: 03cc952f-83fc-4d45-9ee3-b5c18522f922
---

The user is learning how AI works by using it to build production-grade projects. The project (kafka-ai) is a vehicle — the real subject is AI behaviour: how it interprets prompts, when it decides vs asks, how it handles failure, when to override it.

High meta-awareness: documents every prompt, every AI decision, and every architectural call in structured logs. This isn't autocomplete usage — it's a deliberate study.

Values:
- Explanations of "why", not just "what". After any fix or feature, they want the reasoning behind it.
- Thorough documentation: has `AI docs/` folder alongside `docs/` in each project.
- Autonomy: makes all architectural decisions themselves; AI executes.

Roadmap intent: rebuild the same Kafka app in Go, Python, and Node.js to compare AI behaviour across ecosystems — how much guidance each language needs, what tradeoffs AI makes, where it struggles.

Related: [[project-kafka-ai]]
